<table class="table">
  <tr>
    <th>ジャンル検索</th>
  </tr>
  <%= form_with url: genres_genre_path, method: :get do |f| %>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name, genres_genre_path(genre_id: genre.id), method: :get, class: "text-black-50" %></td>
      </tr>
    <% end %>
  <% end %>
</table>